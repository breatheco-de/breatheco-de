import Typography from 'typography'
import altonTheme from 'typography-theme-alton'
altonTheme.baseFontSize = '22px' // was 20px.

const typography = new Typography(altonTheme);

export default typography;