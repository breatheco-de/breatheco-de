/* global require */
export const querySelector = require('./querySelector').default;
export const addEventListener = require('./addEventListener').default;
export const window = require('./window').default;