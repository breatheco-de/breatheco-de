import { StateMachine, EventObject, StateValue } from './types';
export interface ReachesOptions {
    filter?: (state: {
        value: StateValue;
        context: any;
    }) => boolean;
}
export declare function reaches<TC, TE extends EventObject>(machine: StateMachine<TC, any, TE>, stateValue: StateValue, options?: ReachesOptions): boolean;
