"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var index_1 = require("../../src/index");
var utils_1 = require("../utils");
describe('Example 6.17', function () {
    var machine = index_1.Machine({
        initial: 'X',
        states: {
            X: {
                on: {
                    1: 'Y',
                    2: 'Y.A.C',
                    // 3: { Y: { A: 'C', B: 'F' } } // 6.19
                    4: 'Y.A.hist'
                }
            },
            Y: {
                type: 'parallel',
                states: {
                    A: {
                        initial: 'D',
                        states: { C: {}, D: {}, E: {}, hist: { history: true } }
                    },
                    B: {
                        initial: 'G',
                        states: { F: {}, G: {}, H: {} }
                    }
                },
                on: {
                    back: 'X'
                }
            }
        }
    });
    var expected = {
        X: {
            1: { Y: { A: 'D', B: 'G' } },
            2: { Y: { A: 'C', B: 'G' } },
            // 3: { Y: { A: 'C', B: 'F' } }, //  6.19
            '2, back, 4': { Y: { A: 'C', B: 'G' } }
        },
        '{"Y":{"A":"C","B":"G"}}': {
            back: 'X'
        },
        'Y.A.C': {
            back: 'X'
        },
        'Y.B.G': {
            back: 'X'
        }
    };
    Object.keys(expected).forEach(function (fromState) {
        Object.keys(expected[fromState]).forEach(function (eventTypes) {
            var toState = expected[fromState][eventTypes];
            it("should go from " + fromState + " to " + JSON.stringify(toState) + " on " + eventTypes, function () {
                var resultState = utils_1.testMultiTransition(machine, fromState, eventTypes);
                chai_1.assert.deepEqual(resultState.value, toState);
            });
        });
    });
});
describe('Jump to ID', function () {
    var machine = index_1.Machine({
        initial: 'X',
        states: {
            X: {
                id: 'X',
                on: {
                    1: 'Y',
                    2: 'Y.A.C',
                    // 3: { Y: { A: 'C', B: 'F' } } // 6.19
                    4: 'Y.A.hist'
                }
            },
            Y: {
                type: 'parallel',
                states: {
                    A: {
                        initial: 'D',
                        states: {
                            C: {
                                on: {
                                    finish: '#X'
                                }
                            },
                            D: {},
                            E: {},
                            hist: { history: true }
                        }
                    },
                    B: {
                        initial: 'G',
                        states: { F: {}, G: {}, H: {} }
                    }
                },
                on: {
                    kill: '#X'
                }
            }
        }
    });
    var expected = {
        'Y.B.G': {
            kill: 'X'
        },
        '{"Y":{"A":"C","B":"H"}}': {
            finish: 'X'
        }
    };
    Object.keys(expected).forEach(function (fromState) {
        Object.keys(expected[fromState]).forEach(function (eventTypes) {
            var toState = expected[fromState][eventTypes];
            it("should go from " + fromState + " to " + JSON.stringify(toState) + " on " + eventTypes, function () {
                var resultState = utils_1.testMultiTransition(machine, fromState, eventTypes);
                chai_1.assert.deepEqual(resultState.value, toState);
            });
        });
    });
});
