interface EventProcessorOptions {
    deferEvents: boolean;
}
export declare class EventProcessor {
    private processingEvent;
    private queue;
    private initialized;
    private options;
    constructor(options?: Partial<EventProcessorOptions>);
    initialize(callback?: () => void): void;
    processEvent(callback: () => void): void;
    private flushEvents;
    private process;
}
export {};
